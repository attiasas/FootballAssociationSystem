package DL.Game.MatchEvents;

import java.util.Date;

public class EndGame extends Event {

    public EndGame(int gameTime, Date timeStamp, String description) {
        super(gameTime, timeStamp, description);
    }
}
